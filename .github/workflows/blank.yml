name: CI

on: 
  push:
    branches: source

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        ref: source
    - name: Run Subtree
      run: |
        git branch
        ls
